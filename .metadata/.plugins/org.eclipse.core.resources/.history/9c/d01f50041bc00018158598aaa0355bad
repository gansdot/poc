spring:
  application:
    name: debit-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8090/eureka 
  instance:
    instance-id: ${spring.application.name}

server:
  port: 8092